# Default TOML template for LLM API Wrapper
[meta]
model = "moonshotai/kimi-k2:free"
purpose = "smoke-test"
temperature = 0.7
max_tokens = 256

[[messages]]
role = "user"
content = "A liminal rumination from the perspective of an LLM (sonnet form)"
